name: smarty-ci

# Maintenance baseline plus complete backend project checks. Publication is gated
# by source/workflow/cost admission; this definition does not authorize dispatch.
on:
  pull_request:
    branches: [codex/maintenance-0.153.4]
  push:
    branches: [codex/maintenance-0.153.4, ci/sense19-generators]

permissions:
  contents: read

concurrency:
  group: codex-maintenance-0.153.4
  cancel-in-progress: false

jobs:
  maintenance-baseline:
    if: github.repository == 'Smarty-Pants-Inc/codex' && github.ref != 'refs/heads/ci/sense19-generators'
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    env:
      CARGO_BUILD_JOBS: 2
      CARGO_PROFILE_DEV_DEBUG: 0
      CARGO_PROFILE_TEST_DEBUG: 0
      CARGO_NET_GIT_FETCH_WITH_CLI: true
      INSTA_UPDATE: 'no'
      TARGET: x86_64-unknown-linux-gnu
      USE_SCCACHE: 'true'
      CARGO_INCREMENTAL: '0'
      SCCACHE_CACHE_SIZE: 10G
      # Long tests/link steps must not expire the job-local server and reset its counters.
      SCCACHE_IDLE_TIMEOUT: '0'
      FILTER: 'test(realtime_) | test(history_replay) | test(replay_thread_snapshot) | test(rollout_history) | test(session_lifecycle_requests) | test(thread_event) | test(reconnect) | test(tokens) | test(exec_flow)'
    defaults:
      run:
        shell: bash
    steps:
      - name: Set disposable build directory
        run: echo "CARGO_TARGET_DIR=$RUNNER_TEMP/codex-target" >> "$GITHUB_ENV"
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          # PR: GitHub's combined merge revision. Push: the exact landed commit.
          ref: ${{ github.sha }}
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          components: rustfmt, clippy
      - uses: taiki-e/install-action@44c6d64aa62cd779e873306675c7a58e86d6d532 # v2.62.49
        with:
          tool: just@1.51.0,nextest@0.9.103
      - name: Native Linux test prerequisites (disposable runner only)
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends build-essential pkg-config libcap-dev bubblewrap
          sudo sysctl -w kernel.unprivileged_userns_clone=1
          sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
      - name: Record exact source and toolchain
        run: |
          set -euo pipefail
          mkdir -p evidence package
          jq -n --arg commit "$(git rev-parse HEAD)" --arg tree "$(git rev-parse 'HEAD^{tree}')" \
            --arg definition "${GITHUB_WORKFLOW_SHA}" --arg run "${GITHUB_RUN_ID}" \
            --arg attempt "${GITHUB_RUN_ATTEMPT}" --arg job "${GITHUB_JOB}" \
            --arg target "$TARGET" --arg filter "$FILTER" \
            '{commit:$commit,tree:$tree,definition:$definition,run:$run,attempt:$attempt,job:$job,target:$target,filter:$filter,retries:0}' > evidence/source.json
          { rustc -Vv; cargo -V; just --version; cargo nextest --version; uname -a; } > evidence/tools.txt
          sha256sum codex-rs/Cargo.lock codex-rs/rust-toolchain.toml .github/workflows/smarty-ci.yml > evidence/inputs.sha256
      - name: Install sccache
        if: ${{ env.USE_SCCACHE == 'true' }}
        uses: taiki-e/install-action@44c6d64aa62cd779e873306675c7a58e86d6d532 # v2.62.49
        with:
          tool: sccache@0.7.5
      - name: Verify sccache version
        if: ${{ env.USE_SCCACHE == 'true' }}
        run: |
          version="$(sccache --version)"
          printf '%s\n' "$version" | tee evidence/sccache-version.txt
          test "$version" = 'sccache 0.7.5'
      - name: Configure sccache backend
        if: ${{ env.USE_SCCACHE == 'true' }}
        run: |
          set -euo pipefail
          if [[ -n "${ACTIONS_CACHE_URL:-}" && -n "${ACTIONS_RUNTIME_TOKEN:-}" ]]; then
            echo "SCCACHE_GHA_ENABLED=true" >> "$GITHUB_ENV"
            echo "Using sccache GitHub backend"
          else
            echo "SCCACHE_GHA_ENABLED=false" >> "$GITHUB_ENV"
            echo "SCCACHE_DIR=${{ github.workspace }}/.sccache" >> "$GITHUB_ENV"
            echo "Using sccache local disk + actions/cache fallback"
          fi
      - name: Enable sccache wrapper
        if: ${{ env.USE_SCCACHE == 'true' }}
        run: echo "RUSTC_WRAPPER=sccache" >> "$GITHUB_ENV"
      - name: Restore sccache cache (fallback)
        if: ${{ env.USE_SCCACHE == 'true' && env.SCCACHE_GHA_ENABLED != 'true' }}
        id: cache_sccache_restore
        uses: actions/cache/restore@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: ${{ github.workspace }}/.sccache/
          key: sccache-smarty-v2-0.7.5-ubuntu24-x64-gnu-${{ github.job }}-${{ hashFiles('codex-rs/Cargo.lock', 'codex-rs/rust-toolchain.toml') }}-${{ github.run_id }}
          restore-keys: |
            sccache-smarty-v2-0.7.5-ubuntu24-x64-gnu-${{ github.job }}-${{ hashFiles('codex-rs/Cargo.lock', 'codex-rs/rust-toolchain.toml') }}-
      - name: Record compiler cache restore
        if: always()
        env:
          CACHE_OUTCOME: ${{ steps.cache_sccache_restore.outcome }}
          CACHE_PRIMARY_KEY: ${{ steps.cache_sccache_restore.outputs.cache-primary-key }}
          CACHE_MATCHED_KEY: ${{ steps.cache_sccache_restore.outputs.cache-matched-key }}
          CACHE_EXACT_HIT: ${{ steps.cache_sccache_restore.outputs.cache-hit }}
        run: |
          mkdir -p evidence
          jq -n '{event:env.GITHUB_EVENT_NAME,ref:env.GITHUB_REF,
            gha_backend:env.SCCACHE_GHA_ENABLED,outcome:env.CACHE_OUTCOME,
            primary_key:env.CACHE_PRIMARY_KEY,matched_key:env.CACHE_MATCHED_KEY,
            exact_hit:env.CACHE_EXACT_HIT}' > evidence/cache-restore.json
      - name: Format check
        working-directory: codex-rs
        run: cargo fmt -- --config imports_granularity=Item --check
      - name: Discover exact focused test roster
        working-directory: codex-rs
        run: |
          set -euo pipefail
          cargo nextest list --locked -p codex-tui --message-format json > ../evidence/discovered.json
          cargo nextest list --locked -p codex-tui -E "$FILTER" --message-format json > ../evidence/selected.json
      - name: Focused native regressions, without retries or snapshot updates
        run: |
          set -euo pipefail
          just test --locked -p codex-tui -E "$FILTER" --retries 0 --test-threads 2 \
            --status-level all --final-status-level all 2>&1 | tee evidence/tests.txt
      - name: Scoped Clippy
        run: just clippy --locked -p codex-tui -- -D warnings
      - name: Configure matching sandbox V8 artifacts for the release package
        uses: ./.github/actions/setup-rusty-v8
        with:
          target: x86_64-unknown-linux-gnu
      - name: Build and finalize bundled bwrap before compiling its digest into Codex
        working-directory: codex-rs
        run: |
          set -euo pipefail
          cargo build --locked --release --target "$TARGET" -p codex-bwrap --bin bwrap
          bwrap_path="$CARGO_TARGET_DIR/$TARGET/release/bwrap"
          strip --strip-debug --strip-unneeded "$bwrap_path"
          sha256sum "$bwrap_path" > ../evidence/bwrap.sha256
          digest="$(sha256sum "$bwrap_path" | awk '{print $1}')"
          echo "CODEX_BWRAP_SHA256=$digest" >> "$GITHUB_ENV"
      - name: Build exact staged GNU frontend and native companions
        working-directory: codex-rs
        run: |
          sha256sum "$RUSTY_V8_ARCHIVE" "$RUSTY_V8_SRC_BINDING_PATH" > ../evidence/rusty-v8.sha256
          cargo build --locked --release --target "$TARGET" \
            -p codex-cli --bin codex \
            -p codex-code-mode-host --bin codex-code-mode-host
      - name: Package frontend and inspect ELF requirements, without activation
        run: |
          set -euo pipefail
          release="$CARGO_TARGET_DIR/$TARGET/release"
          CODEX_REPO_ROOT="$GITHUB_WORKSPACE" python3 scripts/build_codex_package.py \
            --target "$TARGET" --variant codex --cargo-profile release --package-dir package \
            --entrypoint-bin "$release/codex" \
            --code-mode-host-bin "$release/codex-code-mode-host" --bwrap-bin "$release/bwrap"
          sha256sum --check evidence/bwrap.sha256
          printf '%s  %s\n' "$CODEX_BWRAP_SHA256" package/codex-resources/bwrap | sha256sum --check
          git diff --exit-code
          test -z "$(git ls-files --others --exclude-standard -- codex-rs .github scripts)"
          cp evidence/source.json package/source.json
          { file package/bin/codex; readelf -l package/bin/codex; readelf -d package/bin/codex; } > evidence/elf.txt
          sha256sum package/bin/codex > evidence/binary.sha256
          find package -type f -print0 | sort -z | xargs -0 sha256sum > evidence/package-files.sha256
          sha256sum scripts/build_codex_package.py scripts/codex_package/*.py \
            scripts/codex_package/rg scripts/codex_package/codex-zsh \
            .github/scripts/check_codex_package.py >> evidence/inputs.sha256
          tar -czf "evidence/codex-frontend-$TARGET.tar.gz" -C package .
          sha256sum "evidence/codex-frontend-$TARGET.tar.gz" > evidence/archive.sha256
      - name: Preflight the packaged native runtime without a model call
        run: |
          CODEX_REPO_ROOT="$GITHUB_WORKSPACE" python3 -m unittest discover \
            -s .github/scripts -p test_check_codex_package.py
          CODEX_REPO_ROOT="$GITHUB_WORKSPACE" python3 .github/scripts/check_codex_package.py \
            "evidence/codex-frontend-$TARGET.tar.gz" "$TARGET" evidence/package-preflight.json
      - name: Save sccache cache (fallback)
        id: cache_sccache_save
        if: always() && !cancelled() && env.USE_SCCACHE == 'true' && env.SCCACHE_GHA_ENABLED != 'true'
        continue-on-error: true
        uses: actions/cache/save@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: ${{ github.workspace }}/.sccache/
          key: sccache-smarty-v2-0.7.5-ubuntu24-x64-gnu-${{ github.job }}-${{ hashFiles('codex-rs/Cargo.lock', 'codex-rs/rust-toolchain.toml') }}-${{ github.run_id }}
      - name: sccache stats
        if: always() && env.USE_SCCACHE == 'true'
        continue-on-error: true
        env:
          CACHE_SAVE_OUTCOME: ${{ steps.cache_sccache_save.outcome }}
        run: |
          mkdir -p evidence
          jq -n '{outcome:env.CACHE_SAVE_OUTCOME}' > evidence/cache-save.json
          sccache --show-stats 2>&1 | tee evidence/sccache.txt || true
      - name: sccache summary
        if: always() && env.USE_SCCACHE == 'true'
        run: |
          {
            echo "### sccache stats — x86_64-unknown-linux-gnu (maintenance-baseline)";
            echo;
            echo '```';
            sccache --show-stats || true;
            echo '```';
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Retain test results and actual counts
        if: always()
        run: |
          mkdir -p evidence
          if [[ -f codex-rs/target/nextest/local/junit.xml ]]; then
            cp codex-rs/target/nextest/local/junit.xml evidence/junit.xml
          fi
          python3 - <<'PY'
          import json
          from pathlib import Path
          import xml.etree.ElementTree as ET
          root = Path('evidence')
          counts = {}
          for name in ('discovered', 'selected'):
              path = root / f'{name}.json'
              if path.exists():
                  try:
                      data = json.loads(path.read_text())
                  except json.JSONDecodeError:
                      counts[f'{name}_error'] = 'Incomplete or invalid discovery JSON; counts unknown'
                      continue
                  cases = [case for suite in data['rust-suites'].values() for case in suite['testcases'].values()]
                  counts[name] = len(cases) if name == 'discovered' else sum(case['filter-match']['status'] == 'matches' for case in cases)
                  counts[f'{name}_ignored'] = sum(case['ignored'] for case in cases if name == 'discovered' or case['filter-match']['status'] == 'matches')
                  if name == 'selected':
                      counts['filtered_out'] = sum(case['filter-match'].get('reason') == 'expression' for case in cases)
          path = root / 'junit.xml'
          if path.exists():
              cases = ET.parse(path).findall('.//testcase')
              counts['executed'] = sum(case.find('skipped') is None for case in cases)
              counts['skipped_in_junit'] = sum(case.find('skipped') is not None for case in cases)
              counts['failed'] = sum(case.find('failure') is not None or case.find('error') is not None for case in cases)
          (root / 'counts.json').write_text(json.dumps(counts, indent=2) + '\n')
          PY
      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        id: artifact
        if: always()
        with:
          name: codex-native-${{ github.sha }}-${{ github.run_attempt }}
          path: evidence/
          retention-days: 1
          if-no-files-found: error

  backend:
    if: github.repository == 'Smarty-Pants-Inc/codex' && (github.ref != 'refs/heads/ci/sense19-generators' || (github.event_name == 'push' && !github.event.deleted))
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    env:
      CAPACITY_COMBINED: ${{ github.event_name == 'push' && github.ref == 'refs/heads/ci/sense19-generators' }}
      CARGO_BUILD_JOBS: 2
      CARGO_PROFILE_DEV_DEBUG: 0
      CARGO_PROFILE_TEST_DEBUG: 0
      CARGO_NET_GIT_FETCH_WITH_CLI: true
      INSTA_UPDATE: 'no'
      PROJECTS: 'codex-protocol codex-state codex-goal-extension codex-core core_test_support codex-app-server'
      USE_SCCACHE: 'true'
      CARGO_INCREMENTAL: '0'
      SCCACHE_CACHE_SIZE: 10G
      # Keep cumulative compiler counters across the long core/app-server test phases.
      SCCACHE_IDLE_TIMEOUT: '0'
      # Command-tree timings, not pure compile/test time. The *-exit.txt files own status, including signals.
      CI_TIME_FORMAT: '{"elapsed_seconds":%e,"user_seconds":%U,"system_seconds":%S,"max_rss_kib":%M}'
    defaults:
      run:
        shell: bash
    steps:
      - name: Set disposable backend build directory
        run: echo "CARGO_TARGET_DIR=$RUNNER_TEMP/codex-backend-target" >> "$GITHUB_ENV"
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.sha }}
          fetch-depth: ${{ env.CAPACITY_COMBINED == 'true' && 3 || 1 }}
          persist-credentials: false
      - name: Verify fixed repair source and retain definition
        if: ${{ env.CAPACITY_COMBINED == 'true' }}
        id: repair_source
        run: |
          set -euo pipefail
          source_exit=0
          bash -euxo pipefail > "$RUNNER_TEMP/capacity-combined-source.stdout" 2> "$RUNNER_TEMP/capacity-combined-source.stderr" <<'SH' || source_exit=$?
          test "$GITHUB_EVENT_NAME" = push
          test "$GITHUB_REF" = refs/heads/ci/sense19-generators
          test "$RUNNER_OS" = Linux
          test "$RUNNER_ARCH" = X64
          test "$(uname -m)" = x86_64
          test "$GITHUB_SHA" = "$GITHUB_WORKFLOW_SHA"
          test "$(git rev-parse HEAD)" = "$GITHUB_WORKFLOW_SHA"
          test "$(git show -s --format=%P HEAD)" = 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          test "$(git show -s --format=%P HEAD^)" = '397b3d1a6f517f14ae924b8696a60deefee3d376'
          test "$(git diff --name-only HEAD^ HEAD)" = '.github/workflows/smarty-ci.yml'
          test -z "$(git status --porcelain)"
          definition_tree="$(git rev-parse 'HEAD^{tree}')"
          cp .github/workflows/smarty-ci.yml "$RUNNER_TEMP/capacity-combined-definition.yml"
          git checkout --detach 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          test "$(git rev-parse HEAD)" = 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          test "$(git rev-parse 'HEAD^{tree}')" = '693afb709dec5374190b7ee287c838410d9c1896'
          test -z "$(git status --porcelain)"
          jq -n --arg definition "$GITHUB_WORKFLOW_SHA" --arg definitionTree "$definition_tree" --arg definitionParent 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3' \
            --arg source 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3' --arg tree '693afb709dec5374190b7ee287c838410d9c1896' \
            --arg run "$GITHUB_RUN_ID" --arg attempt "$GITHUB_RUN_ATTEMPT" --arg job "$GITHUB_JOB" \
            --arg event "$GITHUB_EVENT_NAME" --arg ref "$GITHUB_REF" \
            --arg runner "$RUNNER_NAME" --arg os "$RUNNER_OS" --arg arch "$RUNNER_ARCH" \
            --arg account "$(id -un)" --arg uid "$(id -u)" \
            '{scope:"focused capacity checks only, not affected-package completion or H/Q/L",definition:$definition,definitionTree:$definitionTree,definitionParent:$definitionParent,
              source:$source,tree:$tree,run:$run,attempt:$attempt,job:$job,event:$event,ref:$ref,
              runner:$runner,os:$os,arch:$arch,account:$account,uid:$uid,cleanInput:true,
              testGroups:[{group:"codex-core",package:"codex-core",target:"all"},
                {group:"codex-app-server",package:"codex-app-server",target:"all"}]}' > "$RUNNER_TEMP/capacity-combined-input.json"
          SH
          mkdir -p backend-evidence/capacity-combined
          cp "$RUNNER_TEMP"/capacity-combined-source.stdout "$RUNNER_TEMP"/capacity-combined-source.stderr backend-evidence/capacity-combined/
          printf '%s\n' "$source_exit" > backend-evidence/capacity-combined/source-exit.txt
          test "$source_exit" = 0
          cp "$RUNNER_TEMP"/capacity-combined-definition.yml "$RUNNER_TEMP"/capacity-combined-input.json backend-evidence/capacity-combined/
          sha256sum backend-evidence/capacity-combined/capacity-combined-definition.yml codex-rs/Cargo.lock MODULE.bazel.lock \
            justfile scripts/just-shell.py > backend-evidence/capacity-combined/input.sha256
          echo 'PROJECTS=codex-core codex-app-server' >> "$GITHUB_ENV"
          echo 'TEST_GROUPS=codex-core codex-app-server' >> "$GITHUB_ENV"
          cat > backend-evidence/capacity-combined/phase.sh <<'SH'
          phase() {
            local name="$1"; shift
            local output="$GITHUB_WORKSPACE/backend-evidence/capacity-combined/$name"
            local code=0
            mkdir -p "$output"
            printf '%q ' "$@" > "$output/command.txt"
            date -u +%FT%TZ > "$output/started.txt"
            /usr/bin/time -q -f "$CI_TIME_FORMAT" -o "$output/time.json" "$@" \
              > "$output/stdout.txt" 2> "$output/stderr.txt" || code=$?
            printf '%s\n' "$code" > "$output/exit.txt"
            date -u +%FT%TZ > "$output/completed.txt"
            if [[ "$code" != 0 ]]; then
              if [[ ! -f "$GITHUB_WORKSPACE/backend-evidence/capacity-combined/first-failure.txt" ]]; then
                printf '%s %s\n' "$name" "$code" > "$GITHUB_WORKSPACE/backend-evidence/capacity-combined/first-failure.txt"
              fi
              exit "$code"
            fi
          }
          SH
          cat > backend-evidence/capacity-combined/verify-roster.py <<'PY'
          """Validate future native discovery and JUnit; authored anchors are requirements only."""
          import json
          from pathlib import Path
          import sys
          import xml.etree.ElementTree as ET

          project, phase = sys.argv[1:]
          root = Path('backend-evidence') / project
          binary = project + '::all'
          controls = {
              'suite::client::usage_limit_error_emits_rate_limit_event',
              'suite::client::provider_auth_command_refreshes_after_401',
              'suite::external_auth::external_auth_401_retry_uses_refreshed_chatgpt_headers',
          }
          core = 'suite::retry_after::capacity::'
          goal = 'suite::v2::keep_working_tests::capacity_retry::capacity_retry_preserves_goal_status_until_real_settlement::'
          core_cases = {
              'streamed_capacity_preserves_tool_result_and_queued_steering': (
                  'overloaded_recovers', 'slow_down_recovers', 'exhaustion_keeps_steering_unadmitted'),
              'http_capacity_never_renews_sampling_allowance': (
                  'http_zero', 'http_exhaustion', 'stream_then_http_zero', 'stream_then_http_exhaustion'),
              'noncapacity_stream_errors_remain_terminal': ('quota', 'billing', 'policy'),
              'websocket_capacity_recovers_without_http_fallback': ('overloaded', 'slow_down'),
          }
          if project == 'codex-core':
              prefixes = ('suite::retry_after::', 'suite::websocket_fallback::')
              required = controls | {core + 'interrupt_cancels_capacity_backoff_without_another_sample'}
              required |= {core + name + '::' + case for name, cases in core_cases.items() for case in cases}
              required |= {'suite::retry_after::' + name for name in (
                  'sse_overload_with_retry_after_exhausts_stream_retries',
                  'sse_overload_without_retry_after_is_terminal',
                  'websocket_overload_with_nested_retry_after_exhausts_stream_retries',
                  'websocket_overload_without_retry_after_is_terminal',
              )}
          elif project == 'codex-app-server':
              prefixes = ('suite::v2::keep_working_tests::capacity_retry::',)
              controls = set()
              required = {goal + case for case in (
                  'active_recovers', 'active_exhausts', 'paused_recovers', 'paused_exhausts')}
          else:
              raise ValueError(f'Unexpected project: {project}')


          def cases(path):
              data = json.loads(path.read_text())
              suites = data['rust-suites']
              if set(suites) != {binary}:
                  raise ValueError('Expected exactly the selected all integration target')
              suite = suites[binary]
              if (suite['package-name'], suite['binary-id'], suite['binary-name'], suite['kind'], suite['status']) != (
                      project, binary, 'all', 'test', 'listed'):
                  raise ValueError('Wrong package/binary/target or missing native listing')
              return suite['testcases']


          full = cases(root / 'full-discovered.json')
          expected = {name for name in full if name.startswith(prefixes) or name in controls}
          if any(full[name]['ignored'] or full[name]['filter-match']['status'] != 'matches' for name in expected):
              raise ValueError('Ignored or unmatched full-discovery case in required scope')
          actual = {name: case for name, case in cases(root / 'discovered.json').items()
                    if case['filter-match']['status'] == 'matches'}
          if not required <= expected or set(actual) != expected or any(actual[name]['ignored'] for name in expected):
              raise ValueError('Missing required native cases, ignored cases, or selected/full discovery mismatch')
          if not all(any(name.startswith(prefix) for name in expected) for prefix in prefixes):
              raise ValueError('An entire required suite is missing')
          if project == 'codex-app-server' and expected != required:
              raise ValueError('Expected all four corrected goal cases only')
          roster = [{'binary_id': binary, 'test_name': name} for name in sorted(expected)]
          if phase == 'discovery':
              (root / 'actual-roster.json').write_text(json.dumps(roster, indent=2) + '\n')
          elif phase == 'junit':
              if json.loads((root / 'actual-roster.json').read_text()) != roster:
                  raise ValueError('Retained native roster changed')
              cases = ET.parse(root / 'junit.xml').findall('.//testcase')
              executed = sorted((case.attrib['classname'], case.attrib['name']) for case in cases)
              if executed != [(binary, name) for name in sorted(expected)]:
                  raise ValueError('Missing, extra, duplicate or wrong-target JUnit cases')
              if any(case.find(tag) is not None for case in cases for tag in ('skipped', 'failure', 'error', 'rerunFailure', 'rerunError', 'flakyFailure', 'flakyError')):
                  raise ValueError('Unsuccessful or retried JUnit case')
          else:
              raise ValueError(f'Unexpected phase: {phase}')
          print(json.dumps({'phase': phase, 'native_roster': roster, 'count': len(roster)}, indent=2))
          PY
      - name: Validate CI phase receipt wiring without building Rust
        if: ${{ env.CAPACITY_COMBINED != 'true' }}
        run: python3 -m unittest discover -s .github/scripts -p test_ci_phase_receipts.py
      - name: Record backend initial resources
        run: |
          mkdir -p backend-evidence
          {
            date -u +%FT%TZ
            nproc
            free -b
            df -B1 "$GITHUB_WORKSPACE" "$RUNNER_TEMP"
            if [[ "$CAPACITY_COMBINED" == true ]]; then
              for resource in cpu.max memory.max memory.peak; do
                path="/sys/fs/cgroup/$resource"
                if [[ -r "$path" ]]; then printf '%s=' "$resource"; cat "$path"; fi
              done
            fi
          } > backend-evidence/resources-start.txt 2>&1
      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          components: rustfmt, clippy, rust-src
      - uses: taiki-e/install-action@44c6d64aa62cd779e873306675c7a58e86d6d532 # v2.62.49
        with:
          tool: just@1.51.0,nextest@0.9.103
      - uses: facebook/install-dotslash@1e4e7b3e07eaca387acb98f1d4720e0bee8dbb6a # v2
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: '0.11.3'
          enable-cache: false
      - name: Native Linux backend prerequisites (disposable runner only)
        if: ${{ env.CAPACITY_COMBINED != 'true' }}
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends build-essential pkg-config libcap-dev bubblewrap
          sudo sysctl -w kernel.unprivileged_userns_clone=1
          sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
      - name: Repair compilation prerequisites (disposable runner only)
        if: ${{ env.CAPACITY_COMBINED == 'true' }}
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends build-essential pkg-config libcap-dev
      - name: Record backend source and toolchain
        run: |
          set -euo pipefail
          mkdir -p backend-evidence
          jq -n --arg commit "$(git rev-parse HEAD)" --arg tree "$(git rev-parse 'HEAD^{tree}')" \
            --arg definition "$GITHUB_WORKFLOW_SHA" --arg run "$GITHUB_RUN_ID" \
            --arg attempt "$GITHUB_RUN_ATTEMPT" --arg job "$GITHUB_JOB" --arg projects "$PROJECTS" \
            '{commit:$commit,tree:$tree,definition:$definition,run:$run,attempt:$attempt,job:$job,
              projects:($projects|split(" ")),profile:"local",testThreads:2}' > backend-evidence/source.json
          { rustc -Vv; cargo -V; just --version; cargo nextest --version; uv --version; /usr/bin/time --version; uname -a; } > backend-evidence/tools.txt
          sha256sum codex-rs/Cargo.lock codex-rs/rust-toolchain.toml codex-rs/.config/nextest.toml \
            justfile scripts/format.py scripts/uv.lock sdk/python/uv.lock tools/buildifier \
            .github/workflows/smarty-ci.yml > backend-evidence/inputs.sha256
      - name: Install sccache
        if: ${{ env.USE_SCCACHE == 'true' }}
        uses: taiki-e/install-action@44c6d64aa62cd779e873306675c7a58e86d6d532 # v2.62.49
        with:
          tool: sccache@0.7.5
      - name: Verify sccache version
        if: ${{ env.USE_SCCACHE == 'true' }}
        run: |
          version="$(sccache --version)"
          printf '%s\n' "$version" | tee backend-evidence/sccache-version.txt
          test "$version" = 'sccache 0.7.5'
      - name: Configure sccache backend
        if: ${{ env.USE_SCCACHE == 'true' }}
        run: |
          set -euo pipefail
          if [[ -n "${ACTIONS_CACHE_URL:-}" && -n "${ACTIONS_RUNTIME_TOKEN:-}" ]]; then
            echo "SCCACHE_GHA_ENABLED=true" >> "$GITHUB_ENV"
            echo "Using sccache GitHub backend"
          else
            echo "SCCACHE_GHA_ENABLED=false" >> "$GITHUB_ENV"
            echo "SCCACHE_DIR=${{ github.workspace }}/.sccache" >> "$GITHUB_ENV"
            echo "Using sccache local disk + actions/cache fallback"
          fi
      - name: Enable sccache wrapper
        if: ${{ env.USE_SCCACHE == 'true' }}
        run: echo "RUSTC_WRAPPER=sccache" >> "$GITHUB_ENV"
      - name: Restore sccache cache (fallback)
        if: ${{ env.USE_SCCACHE == 'true' && env.SCCACHE_GHA_ENABLED != 'true' }}
        id: cache_sccache_restore
        uses: actions/cache/restore@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: ${{ github.workspace }}/.sccache/
          key: sccache-smarty-v2-0.7.5-ubuntu24-x64-gnu-${{ github.job }}-${{ hashFiles('codex-rs/Cargo.lock', 'codex-rs/rust-toolchain.toml') }}-${{ github.run_id }}
          restore-keys: |
            sccache-smarty-v2-0.7.5-ubuntu24-x64-gnu-${{ github.job }}-${{ hashFiles('codex-rs/Cargo.lock', 'codex-rs/rust-toolchain.toml') }}-
      - name: Record compiler cache restore
        if: always()
        env:
          CACHE_OUTCOME: ${{ steps.cache_sccache_restore.outcome }}
          CACHE_PRIMARY_KEY: ${{ steps.cache_sccache_restore.outputs.cache-primary-key }}
          CACHE_MATCHED_KEY: ${{ steps.cache_sccache_restore.outputs.cache-matched-key }}
          CACHE_EXACT_HIT: ${{ steps.cache_sccache_restore.outputs.cache-hit }}
        run: |
          mkdir -p backend-evidence
          jq -n '{event:env.GITHUB_EVENT_NAME,ref:env.GITHUB_REF,
            gha_backend:env.SCCACHE_GHA_ENABLED,outcome:env.CACHE_OUTCOME,
            primary_key:env.CACHE_PRIMARY_KEY,matched_key:env.CACHE_MATCHED_KEY,
            exact_hit:env.CACHE_EXACT_HIT}' > backend-evidence/cache-restore.json
      - name: Configure matching sandbox V8 artifacts
        uses: ./.github/actions/setup-rusty-v8
        with:
          target: x86_64-unknown-linux-gnu
      - name: Capacity runtime test prerequisite
        if: ${{ env.CAPACITY_COMBINED == 'true' }}
        id: capacity_runtime
        run: |
          set -euo pipefail
          source "$GITHUB_WORKSPACE/backend-evidence/capacity-combined/phase.sh"
          phase runtime-prerequisites bash -euo pipefail -c 'cd codex-rs; cargo build --locked -p codex-linux-sandbox --bin codex-linux-sandbox; test -x "$CARGO_TARGET_DIR/debug/codex-linux-sandbox"; sha256sum "$CARGO_TARGET_DIR/debug/codex-linux-sandbox"'
      - name: Read-only Core and app-server Clippy
        if: ${{ env.CAPACITY_COMBINED == 'true' }}
        id: repair_checks
        run: |
          set -euo pipefail
          output="$GITHUB_WORKSPACE/backend-evidence/capacity-combined"
          test "$(git rev-parse HEAD)" = 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          index="$RUNNER_TEMP/capacity-combined-before.index"
          trap 'rm -f "$index" "$index.lock"' EXIT
          GIT_INDEX_FILE="$index" git read-tree 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          GIT_INDEX_FILE="$index" git add -A -- . ':!backend-evidence' ':!.sccache'
          GIT_INDEX_FILE="$index" git write-tree > "$output/command-input-tree.txt"
          test "$(cat "$output/command-input-tree.txt")" = '693afb709dec5374190b7ee287c838410d9c1896'
          git diff --cached --exit-code 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          source "$GITHUB_WORKSPACE/backend-evidence/capacity-combined/phase.sh"
          sha256sum "$RUSTY_V8_ARCHIVE" "$RUSTY_V8_SRC_BINDING_PATH" > backend-evidence/rusty-v8.sha256
          phase focused-clippy just clippy --locked -p codex-core -p codex-app-server -- -D warnings
      - name: Bind scoped fix to unchanged source
        if: ${{ env.CAPACITY_COMBINED == 'true' }}
        id: capacity_fix_source
        run: |
          set -euo pipefail
          output="$GITHUB_WORKSPACE/backend-evidence/capacity-combined"
          test "$(git rev-parse HEAD)" = 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          index="$RUNNER_TEMP/capacity-combined-before.index"
          trap 'rm -f "$index" "$index.lock"' EXIT
          GIT_INDEX_FILE="$index" git read-tree 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          GIT_INDEX_FILE="$index" git add -A -- . ':!backend-evidence' ':!.sccache'
          GIT_INDEX_FILE="$index" git write-tree > "$output/fix-input-tree.txt"
          test "$(cat "$output/fix-input-tree.txt")" = '693afb709dec5374190b7ee287c838410d9c1896'
          git diff --cached --exit-code 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
      - name: Scoped Core and app-server fix without accepted writeback
        if: ${{ env.CAPACITY_COMBINED == 'true' }}
        id: capacity_fix
        run: |
          set -euo pipefail
          source "$GITHUB_WORKSPACE/backend-evidence/capacity-combined/phase.sh"
          phase scoped-fix just fix --locked -p codex-core -p codex-app-server -- -D warnings
      - name: Bind repair tests to unchanged source
        if: ${{ env.CAPACITY_COMBINED == 'true' }}
        id: repair_check_source
        run: |
          set -euo pipefail
          output="$GITHUB_WORKSPACE/backend-evidence/capacity-combined"
          test "$(git rev-parse HEAD)" = 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          index="$RUNNER_TEMP/capacity-combined-before.index"
          trap 'rm -f "$index" "$index.lock"' EXIT
          GIT_INDEX_FILE="$index" git read-tree 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          GIT_INDEX_FILE="$index" git add -A -- . ':!backend-evidence' ':!.sccache'
          GIT_INDEX_FILE="$index" git write-tree > "$output/checks-input-tree.txt"
          test "$(cat "$output/checks-input-tree.txt")" = '693afb709dec5374190b7ee287c838410d9c1896'
          git diff --cached --exit-code 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
      - name: Capacity Core and corrected goal integration tests
        if: ${{ env.CAPACITY_COMBINED == 'true' }}
        id: repair_tests
        run: |
          set -euo pipefail
          failed=0
          for group in $TEST_GROUPS; do
            project="$group"
            retry_args=(--retries 0)
            case "$group" in
              codex-core) selection=(--test all -E 'test(suite::retry_after::) | test(suite::websocket_fallback::) | test(=suite::client::usage_limit_error_emits_rate_limit_event) | test(=suite::client::provider_auth_command_refreshes_after_401) | test(=suite::external_auth::external_auth_401_retry_uses_refreshed_chatgpt_headers)') ;;
              codex-app-server) selection=(--test all -E 'test(suite::v2::keep_working_tests::capacity_retry::)') ;;
              *) echo "Unexpected capacity test group: $group" >&2; exit 1 ;;
            esac
            output="$GITHUB_WORKSPACE/backend-evidence/$group"
            mkdir -p "$output"
            printf '%s\n' "${selection[@]}" "${retry_args[@]}" > "$output/selection.txt"
            date -u +%FT%TZ > "$output/started.txt"
            # Each project overwrites the local-profile JUnit path; never reuse a previous result.
            rm -f codex-rs/target/nextest/local/junit.xml
            printf '%q ' cargo nextest list --locked -p "$project" --test all --message-format json > "$output/full-discovery-command.txt"
            full_exit=0
            (cd codex-rs && /usr/bin/time -q -f "$CI_TIME_FORMAT" -o "$output/full-discovery-time.json" \
              cargo nextest list --locked -p "$project" --test all --message-format json) \
              > "$output/full-discovered.json" 2> "$output/full-discovery.txt" || full_exit=$?
            echo "$full_exit" > "$output/full-discovery-exit.txt"
            if [[ "$full_exit" != 0 ]]; then
              failed=1
              if [[ ! -f backend-evidence/capacity-combined/first-failure.txt ]]; then
                printf '%s full-discovery %s\n' "$group" "$full_exit" > backend-evidence/capacity-combined/first-failure.txt
              fi
              continue
            fi
            printf '%q ' cargo nextest list --locked -p "$project" "${selection[@]}" --message-format json > "$output/discovery-command.txt"
            discovery_exit=0
            (cd codex-rs && /usr/bin/time -q -f "$CI_TIME_FORMAT" -o "$output/discovery-time.json" \
              cargo nextest list --locked -p "$project" "${selection[@]}" --message-format json) \
              > "$output/discovered.json" 2> "$output/discovery.txt" || discovery_exit=$?
            echo "$discovery_exit" > "$output/discovery-exit.txt"
            if [[ "${CAPACITY_COMBINED:-false}" == true && "$discovery_exit" != 0 && ! -f backend-evidence/capacity-combined/first-failure.txt ]]; then
              printf '%s discovery %s\n' "$group" "$discovery_exit" > backend-evidence/capacity-combined/first-failure.txt
            fi
            if [[ "$discovery_exit" != 0 ]]; then
              failed=1
              continue
            fi
            roster_exit=0
            python3 backend-evidence/capacity-combined/verify-roster.py "$group" discovery \
              > "$output/discovery-validation.txt" 2>&1 || roster_exit=$?
            echo "$roster_exit" > "$output/discovery-validation-exit.txt"
            if [[ "$roster_exit" != 0 ]]; then
              failed=1
              if [[ ! -f backend-evidence/capacity-combined/first-failure.txt ]]; then
                printf '%s discovery-roster %s\n' "$group" "$roster_exit" > backend-evidence/capacity-combined/first-failure.txt
              fi
              continue
            fi
            printf '%q ' just test --locked -p "$project" "${selection[@]}" "${retry_args[@]}" --test-threads 2 --status-level all --final-status-level all > "$output/test-command.txt"
            test_exit=0
            /usr/bin/time -q -f "$CI_TIME_FORMAT" -o "$output/test-time.json" \
              just test --locked -p "$project" "${selection[@]}" "${retry_args[@]}" --test-threads 2 \
              --status-level all --final-status-level all 2>&1 | tee "$output/tests.txt" || test_exit=$?
            echo "$test_exit" > "$output/test-exit.txt"
            if [[ "${CAPACITY_COMBINED:-false}" == true && "$test_exit" != 0 && ! -f backend-evidence/capacity-combined/first-failure.txt ]]; then
              printf '%s test %s\n' "$group" "$test_exit" > backend-evidence/capacity-combined/first-failure.txt
            fi
            if [[ -s codex-rs/target/nextest/local/junit.xml ]]; then
              cp codex-rs/target/nextest/local/junit.xml "$output/junit.xml"
              roster_exit=0
              python3 backend-evidence/capacity-combined/verify-roster.py "$group" junit \
                > "$output/junit-validation.txt" 2>&1 || roster_exit=$?
              echo "$roster_exit" > "$output/junit-validation-exit.txt"
              if [[ "$roster_exit" != 0 ]]; then
                failed=1
                if [[ ! -f backend-evidence/capacity-combined/first-failure.txt ]]; then
                  printf '%s junit-roster %s\n' "$group" "$roster_exit" > backend-evidence/capacity-combined/first-failure.txt
                fi
              fi
            else
              failed=1
              if [[ "${CAPACITY_COMBINED:-false}" == true && ! -f backend-evidence/capacity-combined/first-failure.txt ]]; then
                printf '%s missing-junit 1\n' "$group" > backend-evidence/capacity-combined/first-failure.txt
              fi
            fi
            date -u +%FT%TZ > "$output/completed.txt"
            if [[ "$test_exit" != 0 ]]; then failed=1; fi
          done
          exit "$failed"
      - name: Retain repair result and reject all source writeback
        if: always() && env.CAPACITY_COMBINED == 'true'
        env:
          STEPS_JSON: ${{ toJSON(steps) }}
        run: |
          set -euo pipefail
          output="$GITHUB_WORKSPACE/backend-evidence/capacity-combined"
          mkdir -p "$output"
          printf '%s\n' "$STEPS_JSON" > "$output/steps.json"
          python3 - <<'PY'
          import json
          from pathlib import Path
          root = Path('backend-evidence/capacity-combined')
          phases = {}
          for name in ('runtime-prerequisites', 'focused-clippy', 'scoped-fix'):
              path = root / name / 'exit.txt'
              phases[name] = int(path.read_text()) if path.exists() else None
          (root / 'phase-exits.json').write_text(json.dumps(phases, indent=2) + '\n')
          PY
          if [[ ! -f "$output/capacity-combined-input.json" ]]; then
            echo 'Input not verified; no output source tree claimed.' > "$output/output-unavailable.txt"
            exit 1
          fi
          code=0
          bash -euxo pipefail > "$output/source-drift.stdout" 2> "$output/source-drift.stderr" <<'SH' || code=$?
          output="$GITHUB_WORKSPACE/backend-evidence/capacity-combined"
          index="$RUNNER_TEMP/capacity-combined-after.index"
          trap 'rm -f "$index" "$index.lock"' EXIT
          export GIT_INDEX_FILE="$index"
          git read-tree 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          git add -A -- . ':!backend-evidence' ':!.sccache'
          tree="$(git write-tree)"
          printf '%s\n' "$tree" > "$output/output-tree.txt"
          git diff --cached --binary --full-index 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3' > "$output/writeback.diff"
          git diff --cached --name-status 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3' > "$output/changed-paths.txt"
          sha256sum "$output/writeback.diff" > "$output/writeback.sha256"
          test "$(git rev-parse HEAD)" = 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          test "$tree" = '693afb709dec5374190b7ee287c838410d9c1896'
          unset GIT_INDEX_FILE
          git diff --cached --exit-code 'ac87f68d3f3723eebcb80fd02c88ce48bec73af3'
          SH
          printf '%s\n' "$code" > "$output/source-drift-exit.txt"
          if [[ "$code" != 0 && ! -f "$output/first-failure.txt" ]]; then
            printf 'source-drift %s\n' "$code" > "$output/first-failure.txt"
          fi
          exit "$code"
      - name: Build backend runtime test prerequisites
        if: ${{ env.CAPACITY_COMBINED != 'true' }}
        working-directory: codex-rs
        run: |
          set -euo pipefail
          sha256sum "$RUSTY_V8_ARCHIVE" "$RUSTY_V8_SRC_BINDING_PATH" \
            > ../backend-evidence/rusty-v8.sha256
          # Package-scoped nextest runs do not build sibling packages' executables.
          cargo build --locked \
            -p codex-cli --bin codex \
            -p codex-rmcp-client --bin test_stdio_server --bin test_streamable_http_server \
            -p codex-code-mode-host --bin codex-code-mode-host \
            -p codex-linux-sandbox --bin codex-linux-sandbox \
            -p codex-shell-escalation --bin codex-execve-wrapper \
            2>&1 | tee ../backend-evidence/runtime-prerequisites.txt
          for binary in codex test_stdio_server test_streamable_http_server codex-code-mode-host codex-linux-sandbox codex-execve-wrapper; do
            path="$CARGO_TARGET_DIR/debug/$binary"
            if [[ ! -x "$path" ]]; then
              echo "Missing runtime test executable: $path" >&2
              exit 1
            fi
            sha256sum "$path" >> ../backend-evidence/runtime-prerequisites.sha256
            echo "CARGO_BIN_EXE_$binary=$path" >> "$GITHUB_ENV"
            if [[ "$binary" == *-* ]]; then
              echo "CARGO_BIN_EXE_${binary//-/_}=$path" >> "$GITHUB_ENV"
            fi
          done
      - name: Complete backend project tests
        if: ${{ env.CAPACITY_COMBINED != 'true' }}
        run: |
          set -euo pipefail
          failed=0
          for project in $PROJECTS; do
            output="$GITHUB_WORKSPACE/backend-evidence/$project"
            mkdir -p "$output"
            date -u +%FT%TZ > "$output/started.txt"
            # Each project overwrites the local-profile JUnit path; never reuse a previous result.
            rm -f codex-rs/target/nextest/local/junit.xml
            discovery_exit=0
            (cd codex-rs && /usr/bin/time -q -f "$CI_TIME_FORMAT" -o "$output/discovery-time.json" \
              cargo nextest list --locked -p "$project" --message-format json) \
              > "$output/discovered.json" 2> "$output/discovery.txt" || discovery_exit=$?
            echo "$discovery_exit" > "$output/discovery-exit.txt"
            if [[ "$discovery_exit" != 0 ]]; then
              failed=1
              continue
            fi
            test_exit=0
            /usr/bin/time -q -f "$CI_TIME_FORMAT" -o "$output/test-time.json" \
              just test --locked -p "$project" --test-threads 2 \
              --status-level all --final-status-level all 2>&1 | tee "$output/tests.txt" || test_exit=$?
            echo "$test_exit" > "$output/test-exit.txt"
            if [[ -s codex-rs/target/nextest/local/junit.xml ]]; then
              cp codex-rs/target/nextest/local/junit.xml "$output/junit.xml"
            else
              failed=1
            fi
            date -u +%FT%TZ > "$output/completed.txt"
            if [[ "$test_exit" != 0 ]]; then failed=1; fi
          done
          exit "$failed"
      - name: Scoped backend Clippy
        if: ${{ env.CAPACITY_COMBINED != 'true' }}
        run: |
          set -euo pipefail
          failed=0
          for project in $PROJECTS; do
            lint_exit=0
            /usr/bin/time -q -f "$CI_TIME_FORMAT" -o "backend-evidence/$project/clippy-time.json" \
              just clippy --locked -p "$project" -- -D warnings \
              2>&1 | tee "backend-evidence/$project/clippy.txt" || lint_exit=$?
            echo "$lint_exit" > "backend-evidence/$project/clippy-exit.txt"
            if [[ "$lint_exit" != 0 ]]; then failed=1; fi
          done
          exit "$failed"
      - name: Full formatter check (pinned Rust and locked Python tools)
        if: ${{ env.CAPACITY_COMBINED != 'true' }}
        run: just fmt-check
      - name: Verify backend source unchanged
        if: ${{ env.CAPACITY_COMBINED != 'true' }}
        run: |
          git diff --exit-code
          test -z "$(git ls-files --others --exclude-standard -- codex-rs .github scripts sdk tools justfile)"
      - name: Retain per-project backend counts
        if: always()
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path
          import xml.etree.ElementTree as ET

          root = Path('backend-evidence')
          root.mkdir(exist_ok=True)
          counts = {}
          projects = os.environ['PROJECTS']
          if os.environ.get('CAPACITY_COMBINED') == 'true':
              projects = os.environ.get('TEST_GROUPS', projects)
          for project in projects.split():
              output = root / project
              result = {}
              for phase in ('discovery', 'test', 'clippy'):
                  path = output / f'{phase}-exit.txt'
                  result[f'{phase}_exit'] = int(path.read_text()) if path.exists() else None
              path = output / 'discovered.json'
              if path.exists():
                  try:
                      data = json.loads(path.read_text())
                      cases = [case for suite in data['rust-suites'].values() for case in suite['testcases'].values()]
                      result['discovered'] = len(cases)
                      result['selected'] = sum(case['filter-match']['status'] == 'matches' for case in cases)
                      result['ignored'] = sum(case['ignored'] for case in cases)
                      result['filtered_out'] = sum(case['filter-match'].get('reason') == 'expression' for case in cases)
                  except (json.JSONDecodeError, KeyError):
                      result['discovery_error'] = 'Incomplete discovery; counts unknown'
              path = output / 'junit.xml'
              if path.exists():
                  cases = ET.parse(path).findall('.//testcase')
                  result['executed'] = sum(case.find('skipped') is None for case in cases)
                  result['skipped_in_junit'] = sum(case.find('skipped') is not None for case in cases)
                  result['failed'] = sum(case.find('failure') is not None or case.find('error') is not None for case in cases)
                  result['passed'] = result['executed'] - result['failed']
              counts[project] = result
          (root / 'counts.json').write_text(json.dumps(counts, indent=2) + '\n')
          PY
      - name: Save sccache cache (fallback)
        id: cache_sccache_save
        if: always() && !cancelled() && env.USE_SCCACHE == 'true' && env.SCCACHE_GHA_ENABLED != 'true'
        continue-on-error: true
        uses: actions/cache/save@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: ${{ github.workspace }}/.sccache/
          key: sccache-smarty-v2-0.7.5-ubuntu24-x64-gnu-${{ github.job }}-${{ hashFiles('codex-rs/Cargo.lock', 'codex-rs/rust-toolchain.toml') }}-${{ github.run_id }}
      - name: sccache stats
        if: always() && env.USE_SCCACHE == 'true'
        continue-on-error: true
        env:
          CACHE_SAVE_OUTCOME: ${{ steps.cache_sccache_save.outcome }}
        run: |
          mkdir -p backend-evidence
          jq -n '{outcome:env.CACHE_SAVE_OUTCOME}' > backend-evidence/cache-save.json
          sccache --show-stats 2>&1 | tee backend-evidence/sccache.txt || true
      - name: sccache summary
        if: always() && env.USE_SCCACHE == 'true'
        run: |
          {
            echo "### sccache stats — x86_64-unknown-linux-gnu (backend)";
            echo;
            echo '```';
            sccache --show-stats || true;
            echo '```';
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Record backend final resources
        if: always()
        run: |
          mkdir -p backend-evidence
          failed=0
          {
            date -u +%FT%TZ || failed=1
            free -b || failed=1
            df -B1 "$GITHUB_WORKSPACE" "$RUNNER_TEMP" || failed=1
            if [[ ! -f backend-evidence/resources-start.txt ]]; then
              echo 'Initial resource receipt absent; initial phase did not complete.'
            fi
            for path in "${CARGO_TARGET_DIR:-}" "$GITHUB_WORKSPACE/backend-evidence"; do
              if [[ -e "$path" ]]; then
                du -sxB1 "$path" || failed=1
              else
                printf 'Path absent (not measured): %s\n' "$path"
              fi
            done
            if [[ "$CAPACITY_COMBINED" == true ]]; then
              for resource in cpu.max memory.max memory.peak; do
                path="/sys/fs/cgroup/$resource"
                if [[ -r "$path" ]]; then printf '%s=' "$resource"; cat "$path"; fi
              done
            fi
          } > backend-evidence/resources-final.txt 2>&1
          exit "$failed"
      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        if: always()
        with:
          name: codex-backend-${{ github.sha }}-${{ github.run_attempt }}
          path: backend-evidence/
          retention-days: 1
          if-no-files-found: error

  required:
    name: smarty-ci
    if: ${{ always() && github.repository == 'Smarty-Pants-Inc/codex' && github.ref != 'refs/heads/ci/sense19-generators' }}
    needs: [maintenance-baseline, backend]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Require baseline and backend success
        env:
          NEEDS: ${{ toJSON(needs) }}
        run: python3 .github/scripts/check_ci_results.py
