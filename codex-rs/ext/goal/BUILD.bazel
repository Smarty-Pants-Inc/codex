load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "goal",
    compile_data = glob([
        "templates/**",
    ]),
    crate_name = "codex_goal_extension",
    integration_compile_data_extra = [
        "src/accounting.rs",
        "src/keep_working.rs",
        "src/keep_working_tests.rs",
        "src/settlement.rs",
        "src/steering.rs",
    ] + glob(["templates/**"]),
)
